# 2026-08-01 巡检刷新 · 来源笔记（Agent 册）

按 `_maintenance/2026-08-01-巡检刷新-执行口径.md` 执行的 B 类刷新。改前整册已存
`raw-data/history/2026-08-01-Agent-讲义.pptx`。零增删页，zip 级文字手术，audit PASS（122 页）。

## 一、第 10 章 · Claude Code sub-agent 深度口径（P0）

**旧口径**：v2.1.172 起可嵌套、深度 5 层封顶（固定不可配）。
**新口径**：默认 3 层、环境变量 `CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH` 可配（v2.1.217 起支持配置、
v2.1.219 起默认 3 层）；「固定 5 层不可配」只是 v2.1.172–216 的历史区间；v2.1.217–218 曾默认 1 层。

**信源（2026-08-01 访问）**：
- https://code.claude.com/docs/en/sub-agents —「Let subagents spawn their own subagents」节原文：
  "By default, a subagent can spawn subagents of its own, up to three layers below the main
  conversation." / "To change the limit, set CLAUDE_CODE_MAX_SUBAGENT_SPAWN_DEPTH"（标注
  min-version 2.1.217）/ 历史注："v2.1.172 through v2.1.216: … up to five layers deep, and the
  limit couldn't be changed. v2.1.217 through v2.1.218: the limit defaulted to one; v2.1.219
  raised the default to three."

**改动位置（PPT，按锚文本定位 slide 文件）**：
| 放映序 | slide 文件 | 旧文 | 新文 |
| --- | --- | --- | --- |
| p104 学习目标 | slide121.xml | 深度上限（Claude Code 5 层 / Codex 默认 1 层） | 深度上限（Claude Code 默认 3 层 / Codex 默认 1 层） |
| p107 三性质卡 | slide118.xml | Claude Code 5 层封顶（固定不可配）；Codex max_depth 默认 1。 | Claude Code 默认 3 层、环境变量可配（曾固定 5 层）；Codex max_depth 默认 1。 |
| p109 对照表 | slide116.xml | 5 层封顶，固定不可配 | 默认 3 层，环境变量可配 |
| p114 本章小结 | slide111.xml | 深度上限（Claude Code 5 层 / Codex 默认 1） | 深度上限（Claude Code 默认 3 层 / Codex 默认 1） |

**网页（Web-version/agent/index.html）3 处**：③深度上限性质卡（带完整版本区间口径）、
两家对照表「嵌套深度」行、费用 QA（q-agent-10）qa-why 括注；「机制与配置核实」标注加
「（深度口径 2026-08-01 刷新）」。成本论据句「没有上限的扇出就是没有上限的账单」保留——
上限仍在（默认 3 层），论据不因可配而失效。

## 二、第 7 章 · Dify 行（P1）

**旧口径**：148k stars、v1.15.0（2026-06）。
**新口径**：151k stars、v1.16.1（2026-07-28）；v1.16.0（2026-07-17）起 Dify Agent 进入
Open Beta（内置代码沙箱 + 技能系统，默认对全员启用）——产品定位级变化，从 LLM 应用底座向
agent 运行时延伸。

**信源（2026-08-01 访问）**：
- https://github.com/langgenius/dify/releases — v1.16.1 发布 2026-07-28、v1.16.0 发布
  2026-07-17（GitHub API releases 接口复核发布时间戳）；v1.16.0 发布说明原文："Agent App
  enters open beta and is now enabled by default for all users…built-in sandbox for code
  execution and shell commands, a skill system…"
- GitHub API `repos/langgenius/dify` — stargazers_count = 150,969（2026-08-01 实查），写作 151k。

**改动位置**：
- PPT p75（slide70.xml）四平台表 Dify 行：定位格「…RAG 一等公民（摄取/切分/检索内置）」→
  「…RAG 一等公民；Dify Agent（Open Beta，v1.16.0 起）」（括注让位给定位级变化，控制格宽）；
  版本格「148k stars、v1.15.0（2026-06）」→「151k stars、v1.16.1（2026-07）」。
  表底「口径截至 2026-07」未动——v1.16.1（07-28）与 151k 均为 7 月末口径，其余三行仍是
  2026-07-10 核实值，改成 2026-08 反而虚报核实面。
- 网页四平台表 Dify 行：定位格保留括注并加「；v1.16.0 起携 Dify Agent（Open Beta）」，
  规模格同步 151k / v1.16.1；「四平台口径核实」标注加「（Dify 行 2026-08-01 刷新）」。

## 三、顺带发现（未动，留给下批）

- **Codex sub-agent 侧口径存疑**：developers.openai.com/codex/subagents 已 308 跳转至
  learn.chatgpt.com/docs/agent-configuration/subagents；新文档不再写 `agents.max_depth`，
  `agents.max_threads` 转为 `agents.max_concurrent_threads_per_session` 的 legacy 别名且
  未给数值默认，`spawn_agents_on_csv` 已消失。库内「max_depth 默认 1 / max_threads 默认 6 /
  spawn_agents_on_csv（实验）」（PPT p107/109/113 + 网页对照表 + MANIFEST Codex 行）均停
  2026-07-17 口径，不在本次任务清单内，未改——建议下批按新文档整体重核 Codex 侧。
