# 2026-08-01 巡检刷新 · 执行口径

设计来源：同日巡检报告（`2026-08-01-巡检报告.md`）的建议动作，用户已批「直接开始」。
本文件固化「怎么改」的边界，供本批全部执行者（含并行智能体）遵守；改动本身按 B 类走。

## 范围

- **内容批（16 册两面）**：P0 九处对客口径 + P1 事实级 + P2 版本号回写，PPT 与网页同批。
  A2A 不动；RAG 只动书单链接。
- **书单批**：OTel 新仓、Modular 署名、Dynamo 新入口、OpenAI/Anthropic/Kong 文档新址；
  NSA PDF 与 openai.com 价格页经真浏览器人工核验**均为活链**（403 是反爬），不替换、只加备用入口。
- **门户小修**：README.md 黑话 3 处、一页纸 MCP 待办销账、MCP 网页眉题去「样板」、收官日期统一 07-22。
- **结构小修**：AIP 的 KServe/Kueue 行、DE 的 OpenLineage/OPA 行从「稳定事实」表挪回时效性事实表。

## PPT 手术边界（本批铁律）

1. **只做文字级 in-place 替换**：zip 级重建容器、逐条复制、仅替换目标 slide XML；
   不增删页、不动形状/版式/rPr，新文字长度与原文相近（约 ±20%），防溢出。
2. **按锚文本定位 slide 文件**（grep ppt/slides/*.xml），**不按放映序页码猜文件号**（插过页的册两者错位）。
3. 目标短语跨 run 拆分时，逐个改 `<a:t>` 片段拼出新文案，run 数与 rPr 保持不动；新文本做 XML 转义。
4. 每改一个 XML 必过 ET 解析（检查项 17 教训）；整册改完跑 `audit_pptx.py` 必须 PASS；
   `kb_deck_text.py --page` 抽验改动页可见文本。渲染目检收尾统一做（soffice）。
5. 改前先 `kb_archive.py <讲义.pptx>` 落档（书单同理）；网页与 MANIFEST 靠 git 留痕（沿既有惯例）。

## 事实纪律

- 进成品的数字**当场核实到官方/一等信源**（厂商发布文、官方文档、监管原文、GitHub releases）；
  横评类只能当线索不能当依据（本次巡检 4 起漏网全因二手信源，教训已登记）。
- 核不到硬源的数字**宁可不写**：如 Cursor ARR 若无一等源，只写收购事实不写 $4B；
  MM 语音成本旧实测价随换代作废，新价核不到就改定性表述。
- MANIFEST 同步回写：事实行新表述 + 核实日期 2026-08-01 + 信源 + 建议复查日；「最后更新」加一行。
- 每册在 `raw-data/` 留 `2026-08-01-巡检刷新-来源笔记.md`（改了什么、依据哪些信源）。

## 内容取舍（有设计空间的两处）

- **MM 视频章**：撤 Sora 旗舰位，注明官方关停时间线（消费端 2026-04-26 已停 / API 2026-09-24，
  OpenAI 帮助页已独立复核）；补位候选（Wan/Runway）**仅在核到官方版本信源时写**，
  否则保留 Seedance/可灵/Veo 三家并注明「Sora 退场后格局重组中」；34 倍价差复核，核不到改定性。
- **FT 第 6 章**：OpenAI 列从「三方法并存」改「平台收摊 + 时间表倒排」；
  时间表以 OpenAI 官方 deprecations/help 页为准，官方没写的日期不进成品。

## 拿不准 5 条的处置

Terminal-Bench 2.1 由 Eval 执行者官方核实后定改否；Gemini Flash-Lite 档位并入 08-31 定点复查；
昇腾 910D 维持「预计」、登记 2026-09 复查；Qwen 许可限定语由 LLM-Train 执行者核到官方源再加；
Cloudflare 画像本次不动。
