# 2026-09-05 改进执行回执

承接 `_maintenance/2026-09-05-全面审视与改进-执行方案.md`。方案不改，偏离都写在这里。

## 环境

- 执行者：DeepSeek Harness 内的编码代理（非 Claude）；能否联网：是；Python：3.9（本机）
- 起始 commit：`ce185f9`；结束 commit：`a54ec40`
- 拍板项 P1–P6 全部按方案默认执行（用户未另行拍板，本回执「待拍板」为空）

## 逐任务

| 任务 | 状态 | 假设与做法一句话 | 验收命令实际输出摘要 | 偏离方案之处 |
| --- | --- | --- | --- | --- |
| A1 | 完成 | check_freshness 加 `--strict` 与宽限分层（宽限读 KB-CONFIG「超期宽限」=45）；新建 freshness.yml（每周一 --strict → 建/关 issue） | `--asof 2026-09-05` EXIT=0 且 67 条「到期待复核」；`--strict` EXIT=1；`--asof 2026-09-30` EXIT=1；dispatch 后 issue #1 出现 | 首版 workflow 用 GITHUB_OUTPUT 管道，bash -e 下退出码写不进去（实测建了错误分支），改单 step 壳逻辑 |
| A2 | 完成 | fresh.html/首页行加 `data-due`，site.js 按浏览器当天重标 + 重写汇总句；build.py 30 天视界改 timedelta、`--check` 加报告型构建日提醒 | build+check EXIT=0；`data-due=` 计数 319；「目前没有过期」计数 0 | 无 |
| A3 | 完成 | dump_pages 抽三章「售前速查」文字，按同页结构补 sp/de/mm-cheatsheet 三节并重排后续小节编号 | 三个 `id="…cheatsheet"` 各 1 处；check_web_chapters EXIT=0 | 速查页数字只在 MANIFEST 有对应事实行时才写；SP 成本卡加 src-cost 来源行 |
| A4 | 完成 | check_html_links 查锚点（含同页）；check_web_chapters 补反向契约（✅ 章节必须有同名小节） | 全绿；反证（临时改 id）EXIT=1 点名 4 条链接后改回 | 无 |
| A5 | 完成 | 24→27、5.2MB 改量级表述、一页纸手写复查表删、季度→月度 8 处 MANIFEST+2 页、19 册→21、9 处已回刷→已按新版更新、3 处条款号删、维护手册门禁段改一行、kb.css/build.py 注释 19 册清、SURFACES 加维护手册 | 四个 grep 全部 0；bump_style_version 升档到 20260723o | 维护手册也被 check_derived_counts 扫「道数」，随 B 阶段统一回刷 |
| A6 | 完成 | _prep 引用映射补两册 10 行（每被引章节一行）、全表核实日期按源 MANIFEST 回刷 | grep ≥8（实为 10）；check_prep_coverage EXIT=0 | 无 |
| A7 | 完成 | gen_source_list 重建后把 `--check` 加进两份清单 | --check EXIT=0；门禁 19→20 | 道数变化触发 CLAUDE/开始使用/gates.yml 提前回刷 20 |
| D1 | 完成 | 10 路取证子代理逐条开页核实 67 条到期事实 + 执行者对抗复核 OpenAI/DeepSeek/xAI/Anthropic 四张官方页 | 巡检报告成文：成立 27 / 已变 32 / 取不到证 5 / 派生 3；对抗复核无推翻 | 无 |
| D2 | 完成 | 32 条已变逐条改 MANIFEST+讲义 zip 级（36+ 处）+网页+data.js；27 条成立回写日期；3 条派生随源；F12 三组对齐；18 册「最后更新」补记 | `Opus 4.8` 三处 grep 0/0/0；20 道全绿 | AIG:15 取不到证但按建议改掉否定式尾句（销掉最不可证的部分）；DE:17 只改 MANIFEST（网页/讲义本就未复述错误断言） |
| D3 | 完成 | 22 行 30→90，逐条理由见巡检报告附录与下节 | 30 天档 67→45（≤45） | 无偏离；两条边界行（Agent:41 榜单、MM:33 性能宣称）按方案「已发生事件/已定局转向」归类降档，理由写入对应 MANIFEST 边界列 |
| D4 | 完成 | 三册半角括号、A2A 断行、21 册书单标签统一两种前缀+探活脚注、8 处失效链接替换、两册新书单 20 链接探活 | `MANIFEST(` 0 册；「链接探活：」21 册；20 道全绿 | 「标准或官方文档」映射为 🛠️实践（官方文档/标准）；书单脚注用「链接可达性复核」已有日期 08-01 的 9 册标 08-01、其余本轮实测标 09-05 |
| B1 | 完成 | 两份清单抽脚本集合比对（--list-only）；check_derived_counts 道数真源改 run_all_gates gate 行数 | 末尾「清单集合一致（本地 20 个脚本 vs gates.yml 20 个脚本）」；反证改 run 后 EXIT=1 并点名 | 无 |
| B2 | 完成 | _lib.py 五函数；8 个脚本迁移；onetime 基线留档 | 归一化 diff 为空（audit 告警行序为集合迭代非确定，逐行同文仅换序） | 先留底后改的基线首份含 B1 未同步的 2 道红，重取基线后比对 |
| B3 | 完成 | tests/ 20 项（保鲜 8、数目反例 4、build 4、ledger 2、audit 2）+ mini/mini-bad 样张；门禁加「脚本自测」；sync 扩 tests | `unittest discover` 20/20 OK；技能包含 tests（66 文件） | mini.pptx 由 kb_deck_build 从 6 页微型 spec 生成（35KB，PASS）；bad 样张 7pt 越界 FAIL |
| B4 | 完成 | 六项：wellformed 扩扫描面+无 article 全判、css 反向、清单提序、deck_source 作用域、scripts_sync 反向、KB-CONFIG 字段契约 | 每项反证后改回均验证；wellformed 首跑抓出 AI-Gateway README 一处真 bug（游离 `</p>`）已修 | 反向 css 首跑清出 3 类：两页 `chip ext` 死类删除、模板占位符过滤、kedges/knodes/over-grace 白名单 |
| B5 | 完成 | check_internal_terms.py 表驱动 13 词 + terms-exemptions.txt | 0 命中；反证（临时回退一处「已回刷」）1 命中后还原 | 无 |
| B6 | 完成 | 矩阵 3.9/3.12、timeout、concurrency、fetch-tags、release-kb 失败回显、re.split maxsplit | CI 两版本绿（见下节矩阵修复） | 首跑发现 concurrency 组不带矩阵维度时两腿互取消，group 加 `-${{ matrix.python-version }}` 修复（追加提交） |
| B7 | 完成 | 豁免注释 4→5、pipefail 注释、make_share 排除 onetime | 全绿 | build.py 三个超长函数按方案不动（E2） |
| B 收尾 | 完成 | 道数 19→22 统一回刷（CLAUDE.md×3、开始使用.html、gates.yml job 名改「门禁」不再写数） | check_derived_counts 16 面一致；22 道全绿 | 无 |
| C1–C10 | 完成 | patrol/init/illustration/SKILL/tools/web-rules/ppt-design/library-* 按方案逐条改；C9 技能文档轴 + 9 条取证豁免；CHANGELOG 速查表与完整记录各加 v10.0；version 10.0 | C 验收 grep 0；check_skill_sync/package 双绿；`skill-v10.0` Release 存在 | 无 |
| R | 完成 | KB-CONFIG 2026.09.05 → stamp 52 页 → 提交 → 推标签 | `kb-v2026.09.05` Release 带 `ai-knowledge-base-2026.09.05.zip`；线上右上角 v2026.09.05 | 无 |

## 门禁道数变化

改前 19 道 → 改后 22 道（A7 +信源清单、B3 +脚本自测、B5 +内部用语）。回刷门面：CLAUDE.md（三处）、开始使用.html（验一遍节）、gates.yml（job 名去数字）、README/一页纸等由 check_derived_counts 16 面扫描确认一致；维护手册不写道数（只写命令）。

## 待拍板

无——P1–P6 全部按默认执行，未遇需要用户决定的新取舍。

## 只有目检看得见的事

- 本轮改过文字的 9 册讲义（ML/LLM/PE/Evaluation/LLM-Training/AIG/AI-Ops/Security/MM/AIC/LLM-Inference/SP）已过 audit（PASS），但按「严格通道」约定，改过的页还需要在 Mac PowerPoint 里渲染翻一遍——方案 §2 点名的「08-02/03 插过 25 页新内容的册子第二遍翻检」是用户侧动作，尚未做。
- audit 对多册仍有历史告警（非令牌色、小字号占比、同题页对），均为此前批次遗留，本轮未新增；修新页时顺手回正即可（B7 口径）。
- 网页改动已由 build.py 重建并全绿；建议浏览器打开 fresh.html 看一眼汇总句（应显示「今天 2026-09-05：已到期 3 条（超宽限 0 条）…」）。

## 没做的与为什么

- 阶段 E（E1–E8）按方案本轮不做。
- 方案 §2 用户侧两件未做（执行者做不了）：桌面端 `kb-monthly-patrol` 09-01 跑过但无产物的原因排查；Mac PowerPoint 第二遍翻检。
- MM:4/12/13、SP:8、AIG:15 五条「取不到证」按纪律不动成品不回写日期（其中 AIG:15 的否定式尾句已在 D2 按建议改写销除）；到期标记继续亮着，下轮（约 10 月初）补证。
